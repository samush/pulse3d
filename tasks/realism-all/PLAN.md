# Реализм всех предметов сцены: GLB-модели и детальная процедурная геометрия

Дата: 2026-09-06, база `8b81344` (после `tasks/realism-living`, PR #3–#10). Статус этапов — в §2.
Образец результата: диван, стулья и стол кухни-гостиной 4 (`tasks/realism-living/PLAN.md`, там же §10 с цифрами).

## 0. Цель и принципы

- Каждый предмет `ITEMS` должен читаться как собранный из реальных деталей: толщины, фаски, зазоры фасадов, ручки,
  ножки, мягкие подушки со скруглением. Палитра остаётся серой; различие материалов — через слоты (§4).
- **Мягкие и сложные формы — GLB** (`models/<id>.glb`, генератор `tools/models/<id>.js`). **Параметрические
  корпуса и мелочь — процедурно** в `items.js` через `b()` и `THREE.*Geometry`. Правило выбора: если форма описывается
  боксами, цилиндрами и extrude с фаской — процедурно; если нужны скругления ≥ 15 мм на многих рёбрах, купол, кант,
  складки, lathe-профиль — GLB.
- Инварианты (не менять): `id`, `pos`, `rot`, `size`, опорная точка (СЗ угол), `fixed`, `attach`, `SCENE_REV`,
  проходы и предупреждения `LAY`, разметка. Все детали внутри `size` (тест `Box3 ≤ size + 1 мм` для процедурных,
  `validateItemGlb` для GLB). Не масштабировать модель «под кадр», не добавлять декор и предметы, не менять палитру,
  не трогать `three.min.js`/`GLTFLoader.js`.
- **Сначала proxy, потом детали.** У предмета без явных `b.phys` физика берётся по одному боксу на меш; любая
  детализация сдвинула бы столкновения и предупреждения расстановки. Перед детализацией объявить `b.phys(...)`,
  повторяющие сегодняшние AABB мешей (как §2 в `tasks/realism-living/PLAN.md`), и зафиксировать число боксов
  тестом. Исключение: настенная мелочь без влияния на проходы (розетки, выключатели, споты, LED) — один `b.phys`
  на предмет, равный текущему габариту.

## 1. Что уже есть (использовать, не переписывать)

| Что | Где | Как |
|---|---|---|
| Загрузка GLB | `items.js`: `glb:'models/x.glb'`, `glbRot`, `loadItemGlb`, `GLB_CACHE` | процедурный `build` остаётся fallback и proxy; модель подменяет меши после `DOMContentLoaded`; один файл на несколько предметов кэшируется и клонируется (общая geometry) |
| Валидация | `validateItemGlb` | warning в консоли: единицы, `Box3` в `size` ±1 см, низ на y=0, pivot СЗ, фасад как у процедурной версии (центроид верхней четверти) |
| Материалы GLB | `GLB_MATS` в `items.js` | имя материала в GLB = слот (§4), подменяется серым материалом концепта |
| Генератор | `tools/models/glb.js` | `rbox(w,h,d,r,x0,y0,z0,{m,step,crown,fold})` скруглённый бокс; `piping(x0,z0,w,d,r,y,radius)` кант по скруглённому прямоугольнику; `cylinder(cx,y0,cz,r,h,seg)`; `toGlb(parts)` → `{buf,triangles}`, parts=`[{geo,mat:'fabric'}]`, один индексированный меш на материал, UV в метрах. Любая `THREE.*Geometry` из core (`ExtrudeGeometry`, `LatheGeometry`, `TubeGeometry`) годится как `geo` после `translate/rotate` в локальные координаты предмета |
| Примеры | `tools/models/sofa.js`, `tools/models/chair.js` | структура скрипта, комментарии, вывод в `models/` |
| Процедурно | `b(x0,x1,y0,y1,z0,z1,mat)` (UV в метрах), `b.phys`, `g.add(new THREE.Mesh(...))` для не-боксов | стол: `ExtrudeGeometry` с bevel, `CylinderGeometry(…,4).rotateY(π/4)` для квадратных конусных ножек |
| Тесты | `tools/check.js` | блоки «realism-living step N» — образцы: число боксов PHYS, загрузка GLB без warning, общая geometry, слоты |
| Снимки | `controls.setPose(px,py,pz,tx,ty,tz)`, `VIZ.set(bool)`, `renderer.info` | скрипт приёмки: `tasks/realism-living/PLAN.md` §10 (ракурсы), скриншоты в `tools/out/` (не в git) |

Blender на машинах нет; генераторы — node + `three.min.js`. Если появится CC0-модель (Poly Haven) с точным габаритом,
можно взять её: источник и лицензия в `models/README.md`.

## 2. Этапы и статус

Этап = отдельный агент, свой worktree от `origin/main`, ветки `realism-<этап>-<шаг>`, каждый значимый шаг — PR,
мержит сам, ребейз при конфликте. Статус здесь правит исполнитель первым PR этапа (`in-progress (<агент>, <дата>)`)
и последним (`done`). Никаких меток в GitHub.

| Этап | Помещения | Предметы (число) | Статус |
|---|---|---|---|
| A | общие helper'ы; 4 кухня-гостиная (остаток), 5 коридор, 7 постирочная | 12 | in-progress (Claude, 2026-09-06) |
| B | 1 детская | 25 | open |
| C | 2 детская | 21 | open |
| D | 3 спальня | 29 | open |
| E | 8 и 9 санузлы | 34 | open |
| F | 6 гардеробная, 10 лоджия | 24 | open |

Зависимости между этапами — только через общие helper'ы и слоты этапа A (§3, §4). Этап A делает их **первым PR**
(в течение первого часа); до его merge остальные этапы делают proxy и модели, а мелочь (розетки/споты) — после.
Общие модели: `models/kidchair.glb` (кресло для `kidchair` и `kidchair2`) делает этап B, этап C только ссылается
на файл (пока файла нет — работает fallback, это не блокер); `models/wc.glb` (`wc`, `wc8`) — этап E, один файл.

## 3. Общие helper'ы (этап A, первый PR)

В `items.js` рядом с `b`:
- `b.plate(x0,x1,y0,y1,z0,z1,mat,{keys=1})` — настенная рамка розетки/выключателя: рамка с фаской 1 мм
  (`ExtrudeGeometry`), внутри клавиша/розетка на 2 мм ниже; ориентация по тонкой стороне `size`
  (0.01 по x или по z). Материал `plastic`. Один `b.phys` на габарит.
- `b.spot(cx,cz,r,mat)` — точечный светильник: кольцо (`TorusGeometry` или `LatheGeometry`) + диск `led`,
  утоплен в потолок y=2.70.
- `b.led(x0,x1,y0,y1,z0,z1)` — LED-лента: полоса `led` 8 мм + алюминиевый профиль `metal` вокруг.
- `b.round(x0,x1,y0,y1,z0,z1,r,mat)` — бокс с фаской r через `ExtrudeGeometry` (фасады, столешницы, полки);
  UV в метрах даёт `ExtrudeGeometry` сам.
Ручки: `b.handle(cx,cy,cz,len,axis)` — скоба Ø8 мм из `TubeGeometry`/`CylinderGeometry`, слот `chrome`.

## 4. Слоты материалов

Существующие: `chrome`, `metal`, `glass`, `fabric`, `emitter` (`led`), `screen`, `wood` (столы), `paint` (каркасы),
`furniture` (по умолчанию). Этап A добавляет в `MATERIALS`, `SLOTS`/`ITEM_MATS` и `GLB_MATS`:
`plastic` (rough 0.4 — рамки розеток, корпуса ламп), `ceramic` (rough 0.12 — сантехника), `acrylic` (rough 0.2 — ванна),
`leather` (rough 0.6 — изголовье, пуфы), `mirror` (rough 0.02, metal 1). Другие этапы новые слоты **не добавляют**,
чтобы не конфликтовать; если чего-то не хватает — берут ближайший и пишут в TODO этого файла.

## 5. Бюджеты и стоимость

Треугольники на предмет: крупный мягкий (кровать, диван) ≤ 12k; кресло/стул/пуф ≤ 3k; сантехника ≤ 4k; корпусная
мебель процедурно ≤ 1k; мелочь ≤ 200. Сцена сейчас ~23k в самом тяжёлом ракурсе; после всех этапов допустимо
≤ 120k. Draw calls: GLB — один меш на материал; процедурно — не дробить фасады на десятки мешей без нужды.
Один файл на повторяющиеся предметы (кресла, унитазы, споты — споты процедурно через helper).

## 6. Порядок работы в этапе

1. Прочитать `CLAUDE.md`, `tasks/realism-living/PLAN.md` (§0–§5, §10), этот файл, свою строку в §7–§12.
2. Снять «до»: ракурсы этапа (§7–§12, `setPose`) в концепте и VIZ, `renderer.info` — в `tools/out/before-*`.
3. PR 1: proxy для всех предметов этапа + тест числа боксов + статус этапа `in-progress`. Ноль визуальных изменений.
4. PR 2…N: по одному-трём предметам (GLB — генератор + модель + `glb:` + тест загрузки без warning; процедурно —
   код + тест «детали внутри size»). После каждого PR `node tools/check.js` зелёный.
5. Последний PR: «после» в тех же ракурсах, таблица результатов в §13 (треугольники/calls/p95 до → после, отклонения),
   статус `done`, `node tools/check.js https://samush.github.io/pulse3d/` после деплоя.
Отчёт в чат после каждого PR: что сделано, номер PR, что дальше.

## 7. Этап A — общие helper'ы, комната 4 (остаток), коридор 5, постирочная 7

Ракурсы: кухня от дивана `12.6,1.6,5.6, 8.6,1.2,2.6`; коридор от входа `8.9,1.6,9.3, 8.9,1.2,6.4`; постирочная из двери.

| id | size | Тип | Что сделать |
|---|---|---|---|
| kitchen | 0.68×2.69×3.59 | P | нижние и верхние фасады отдельными плитами с зазором 3 мм и фаской 1 мм (`b.round`), цоколь утоплен 0.05, столешница 0.04 с фаской 2 мм и свесом 0.02, фартук, мойка (`LatheGeometry`/бокс с бортом) и смеситель (`TubeGeometry`, `chrome`), варочная панель `glass`, ручки-скобы `chrome`, вытяжка/полка |
| tv | 1.3×1.75×0.04 | P | панель 0.02 с рамкой 8 мм, экран `screen`, нижняя планка `metal` |
| console | 1.2×0.75×0.38 | P | корпус с двумя фасадами (зазор 3 мм), push-to-open без ручек, фаска |
| lamp | 0.26×1.94×0.63 | P | кронштейн `metal` Ø12, абажур `LatheGeometry`, лампа `led` |
| wardrobe | 1.77×2.65×0.45 | P | 3–4 двери с зазорами 3 мм, вертикальные ручки-профили `metal`, цоколь |
| entry | 0.325×1.85×0.4 | P | полка с фаской, 2 ящика с зазором, два светильника (`b.spot`-подобные) |
| mirror | 0.025×2.4×0.9 | P | стекло `mirror` 4 мм + подложка, скруглённые углы (`b.round`) |
| pouf | 0.4×0.45×0.6 | G | скруглённый цилиндр/бокс r 60 мм, кант, купол +10 мм; `models/pouf.glb` (общий с `vpouf` по форме нельзя — размеры разные, отдельный файл) |
| washer | 0.6×1.72×0.6 | G | две машины колонной: люк-кольцо (`TorusGeometry`), стекло `glass`, панель, скруглённые углы корпуса r 10 |
| sw5, sw7 | 0.01×0.99×0.08 | P | `b.plate` |
| — | — | — | helper'ы §3, слоты §4, тест helper'ов (розетка внутри size, число мешей) |

## 8. Этап B — детская 1

Ракурсы: от двери `1.2,1.6,6.2, 3.6,1.0,3.2`; кровать крупно `4.9,1.6,5.4, 3.4,1.3,3.2`; стол `2.4,1.6,5.6, 1.6,0.8,4.6`.

| id | size | Тип | Что сделать |
|---|---|---|---|
| kidbed | 2.6×2.6×2.97 | G | каркас платформы с фаской 10 мм, ограждение из стоек и реек (не плиты), матрас с бортом и куполом, подушка, одеяло складкой; лестница-комод: фасады ящиков с зазорами и ручками; полка. proxy уже есть (9 боксов) — не менять |
| kidsofa | 0.75×0.80×1.60 | G | как диван: каркас r 15, подушки r 40 с куполом, кант |
| kidchair | 0.55×0.85×0.55 | G | рабочее кресло: крестовина 5 лучей с роликами, газлифт `chrome`, сиденье и спинка скруглённые, подлокотники; `models/kidchair.glb`, общий с `kidchair2` |
| kiddesk | 2.139×0.72×0.60 | P | столешница с фаской 3 мм, две опоры-панели, царга |
| kidped | 0.42×0.68×0.40 | P | 3 ящика с зазором, ручки, цоколь, ролики |
| kidshelf, kidshelf2, kidshelf3 | — | P | боковины 18 мм, полки 18 мм, задняя стенка 6 мм, ячейки |
| windowseat1 | 0.60×0.65×1.89 | G | корпус с ящиками (P допустимо), матрасик r 30 с куполом |
| kidrug | 1.60×0.01×1.70 | P | скруглённые углы r 30, толщина 8 мм, край |
| curtain | 0.05×2.30×1.79 | G | карниз `metal` + тюль волной (синус по x, `tulle`) |
| projector, screen | — | P | корпус r 10, объектив `glass`; кассета-цилиндр + кромка экрана |
| bra1, bra2, kidlight, track | — | P | кронштейн `metal`, плафон `LatheGeometry`, лампа `led`; трек — профиль + 2 спота |
| sock1–7, sw1 | — | P | `b.plate` |

## 9. Этап C — детская 2

Ракурсы: от двери `12.4,1.6,7.0, 13.6,1.0,9.6`; кровать `11.6,1.6,10.3, 13.2,1.4,8.2`; шведская стенка `13.9,1.6,9.9, 12.0,1.2,10.8`.

| id | size | Тип | Что сделать |
|---|---|---|---|
| kidbed2 | 2.4×2.6×2.97 | G | как kidbed (свой генератор или общий модуль с параметрами в `tools/models/kidbed.js`) |
| kidchair2 | 0.55×0.85×0.55 | G | `glb:'models/kidchair.glb'` из этапа B, только ссылка |
| kiddesk2, deskshelf2 | — | P | столешница с фаской, консоли `metal`; полка 18 мм с бортиком |
| tower2n, tower2s | 0.60×2.70×… | P | корпус, дверцы/ящики с зазорами 3 мм, штанга `chrome`, открытые ячейки |
| windowseat2 | 0.60×0.65×1.70 | G | как windowseat1 |
| gymwall, pullup | — | P | стойки 40×60, перекладины Ø32 `wood`, турник `metal` Ø32 |
| kidrug2 | 1.60×0.02×2.00 | P | мат: скруглённые углы r 40, толщина 20 |
| blind2, desklamp2 | — | P | кассета-цилиндр + планка; лампа: основание, штанга `metal`, плафон lathe |
| bra3, bra4, kidlight2 | — | P | как в этапе B |
| sock9–13, sw2 | — | P | `b.plate` |

## 10. Этап D — спальня 3

Ракурсы: от двери `11.5,1.6,12.0, 13.6,1.0,9.9`; изголовье `13.4,1.6,11.9, 12.6,1.0,9.5`; туалетный столик `12.2,1.6,9.8, 13.9,1.2,11.2`.

| id | size | Тип | Что сделать |
|---|---|---|---|
| mbed | 1.70×1.10×2.20 | G | мягкое изголовье `leather` r 40 с каретной стяжкой (сетка вмятин 0.25), основание с фаской, матрас r 50 с куполом, 2 подушки r 60, покрывало со складкой; ≤ 12k |
| mcab | 1.70×2.70×0.35 | P | 2 дверцы с зазором 3 мм, секция кондиционера с решёткой |
| mconsole | 1.20×0.60×0.35 | P | два ящика, фаска |
| mward | 1.00×2.70×0.60 | P | 2 двери, ниша-полка, ручки-профили |
| vanity | 1.00×0.75×0.45 | P | столешница с фаской, плоский ящик, ножки Ø25 `metal` |
| vmirror | 1.10×1.52×0.03 | P | полукруг `ShapeGeometry`/`ExtrudeGeometry`, слот `mirror` |
| vpouf | 0.40×0.45×0.40 | G | как pouf, свой файл |
| mtv | 0.97×1.63×0.04 | P | как tv |
| mrug | 2.00×0.01×2.00 | P | как kidrug |
| mcurtain | 0.10×2.68×3.017 | G | шторы собраны у краёв: волна складок (синус, 8–10 складок), карниз `metal` |
| bra5–8, mlight, led4, led5 | — | P | плафон lathe + `metal`; `b.led` |
| sock14–20, sw3, sw4, sw6 | — | P | `b.plate` |

## 11. Этап E — санузлы 8 и 9

Ракурсы: санузел 9 от двери `9.9,1.6,12.2, 8.7,1.0,9.7`; санузел 8 от двери `11.0,1.6,12.9, 9.9,1.0,11.0`.

| id | size | Тип | Что сделать |
|---|---|---|---|
| tub | 0.99×0.58×1.60 | G | каплевидная ванна: `LatheGeometry`-профиль борта + внутренняя чаша, экран; `acrylic`; proxy уже есть (5 боксов) — не менять |
| wc, wc8 | 0.36×0.42×0.48 | G | подвесной унитаз: чаша lathe + крышка r 20, `ceramic`; один `models/wc.glb`, ориентация через `glbRot` |
| basin, basindrawer | — | G / P | раковина: плита с чашей (lathe/скруглённая выемка) `ceramic`; ящик push-to-open с зазором |
| basinmixer, tubmixer, mixer8, shower, rain8 | — | P | `TubeGeometry`/цилиндры `chrome`, лейка lathe, короб термостата с фаской |
| bathmirror, towelrail, towel8 | — | P | зеркало r 30 `mirror` + LED-контур `b.led`; полотенцесушитель — перекладины Ø20 `chrome` |
| wcbox, wcbox8, niche8, curb8e, shower8, drain8, glass8 | — | P | короба с фаской 1 мм, ниша с полкой, бортик, трап с решёткой (`metal`, щели), стекло `glass` 8 мм с профилем |
| cove8, fan, fan8, spot1–6 | — | P | карниз-полка с LED, вентилятор — решётка; `b.spot` |
| sock21, sock22, sock24 | — | P | `b.plate` |

## 12. Этап F — гардеробная 6, лоджия 10

Ракурсы: гардеробная от двери `9.3,1.6,7.5, 9.3,1.4,9.4`; лоджия от проёма `14.2,1.6,3.2, 14.6,1.0,5.6`.

| id | size | Тип | Что сделать |
|---|---|---|---|
| wend, wmezz | — | P | стеллаж: боковины 18, полки, штанги Ø25 `chrome`, крючки |
| wpeg, wboard, wstep, wmirror | — | P | перфопанель (сетка отверстий — `map` не нужен: ряд мелких цилиндров ≤ 200 тр. или просто фаска), доска на скобах, стремянка 2 ступени, зеркало `mirror` |
| wlight, led6 | — | P | линейный профиль `metal` + рассеиватель `led`; `b.led` |
| bdesk, itshelf, bshelf | — | P | столешница с фаской, консоли `metal`; полка с бортиком; стеллаж с ящиками |
| bchair | 0.45×0.90×0.45 | G | стул с прямой спинкой: свой `tools/models/bchair.js` на базе `chair.js` (параметры), не масштабировать `chair.glb` |
| blinds10, cable10, blight | — | P | кассеты-цилиндры; кабель-канал с крышкой; линейный светильник |
| led7, sock26–29, sw8, sock25 | — | P | `b.led`, `b.plate` |

## 13. Результаты приёмки по этапам

Заполняет исполнитель каждого этапа: ракурсы, треугольники/calls/p95 до → после, число боксов PHYS, отклонения.

| Этап | Треугольники до → после (тяжёлый ракурс) | Calls | p95 VIZ, мс | PR | Замечания |
|---|---|---|---|---|---|
| A | | | | | |
| B | | | | | |
| C | | | | | |
| D | | | | | |
| E | | | | | |
| F | | | | | |

## TODO

- (пусто; сюда — недостающие слоты и helper'ы, обнаруженные этапами)
